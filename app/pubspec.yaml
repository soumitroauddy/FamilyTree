name: family_tree_app
description: Interactive family tree app for iOS and Android.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  http: ^1.2.0
  shared_preferences: ^2.3.0
  path_provider: ^2.1.3
  provider: ^6.1.2
  image_picker: ^1.1.2
  supabase_flutter: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
